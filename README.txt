# AKARIN v4 index.html

## 追加機能

- 予定ボード
- 大きく見せるカードモード
- 最大3枚まで絵カードを大きく表示
- PCでは横並び
- スマートフォンでは縦並び
- 既存の cards/cards.json と cards フォルダ画像をそのまま使用

## 設置

この `index.html` をGitHubリポジトリ直下の `index.html` に上書きしてください。
`cards/cards.json` と画像ファイルはそのまま使います。
